html
  include ../includes/header.pug
  body
  block content
  div#curve_chart
  
  form.add-ticker(action="/add-ticker",method="post")
    p.error-message=error
    input(name="search-term")
    button.add(onClick="clicked()") add
  
  if(ticker_arr)
    .ticker-container
      each item in ticker_arr
        p=item